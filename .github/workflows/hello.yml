name: Action Sample

on:
  push:
    branches:
       - master  #'Renshu2'
  workflow_dispatch:

jobs:
  print-hello-world:
    runs-on: ubuntu-latest
    if: contains(github.head_ref, 'Renshu2')
    steps:
      - name: Greeting
        run: echo 'Hello world'
