name: Action Sample

on:
  push:
    branches:[ Renshu2 ]  # developブランチにpushされたときにワークフロー実行
  workflow_dispatch:

jobs:
  print-hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: execute echo command
        run: echo "Hello World!"
